# Publishing
- Version bump ${repo}/crates/kitty-lang-*/Cargo.toml (prefer matching version)
- Version bump ${repo}/Cargo.toml (prefer to match crate versions)
- Update ${repo}/Cargo.toml workspace crate versions to match new bumped versions
- Git commit "version bump" 
- cargo publish all the Cargo.toml above (crates and main)
